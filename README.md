# instagram

Next.js app (App Router, TypeScript, Tailwind). The home route is a login screen (UI only; no backend auth yet).

## Scripts

```bash
bun dev          # development server → http://localhost:3000
bun run build    # production build
bun start        # run production server (after build)
bun run lint     # ESLint
```
